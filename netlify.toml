[build]
  publish = "dist/public"
  command = "bun run build"
  publish = "."

[functions]
  directory = "api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"